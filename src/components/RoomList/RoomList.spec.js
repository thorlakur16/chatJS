//define tests for Login
import React from 'react';
import {shallow} from 'enzyme';
import RoomList from './RoomList';

// The socket.io mock must be implemented to test RoomList
describe('Roomlist tests', () => {
    /*let component;
    beforeEach(() => {
        component = shallow(<RoomList/>);
    });

    it('should contain a label Make new room:', () => {
        expect(component.contains(<div>
            {rooms.map(m => ( <div onClick={component.onItemClick} className={'roomListItem'} key={m}>{m} </div> ))}</div>));
    });*/
});