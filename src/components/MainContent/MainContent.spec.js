//define tests for Login
import React from 'react';
import {shallow} from 'enzyme';
import MainContent from './MainContent';

// The socket.io mock must be implemented to test MainContent
describe('MainContent tests', () => {
    /*let component;

    beforeEach(() => {
        component = shallow(<MainContent/>);
    });

    it('should contain a div with text inputWindow', () => {
        expect(component.contains(<div id="inputWindow">inputWindow</div>));
    });*/
});

